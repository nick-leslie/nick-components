import {createFileRoute} from '@tanstack/react-router'

export const Route = createFileRoute('/whatsup/_layout/gameing')({
    component: () => (
        <div>
            this is so nice
        </div>
    )
})